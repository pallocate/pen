package pen.par

import pen.eco.Constants.SLASH

object Constants
{
   /** Directory where application icons are stored. */
   val ICONS_DIR = "dist${SLASH}resources${SLASH}graphix${SLASH}lgpl${SLASH}"
}
